<h1><%= @list.name %></h1>

<ul>
<% @items.each do |item| %>

	<li><%= link_to item.name, [@list, item] %>
		<%= link_to "Update", edit_list_item_path(@list, item) %>
	</li>

<% end %>
</ul> 

<%= link_to 'New Item', new_list_item_path(@list) %>
<%= link_to 'Back to Lists', lists_path %>
<%= link_to 'Item Details', list_items_path(@list) %>