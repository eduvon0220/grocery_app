<ul>
<% @items.each do |item| %>
	<li><%= item.name.titleize %> - <%= number_to_currency (item.price * item.quantity) %></li>
<% end %>
</ul>
<h2><%= @items.sum(:quantity) %> items</h2>
<h1>Total: <%= number_to_currency @items.sum(:price) %></h1>

<%= link_to "Back", list_path(@list) %>